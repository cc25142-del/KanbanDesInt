Dúvidas -- JS

significado de var?
pq no var qualColuna, já foi definido a coluna "a-fazer" ao invés de deixar apenas vazio (= "";), pq posso escolher querer clicar em outra coluna ao invés dessa
significado de E.TARGET e qual o sentido da function
chamando --> "e"

pegar os elementos da página é como se estivesse instanciando os objetos de uma classe, igual é feito no python?

significado de focus()
explicação do addEventListener
explicação do appendChild
explicação disso: 
 "if (listaDeTarefas[posicao].status == "a-fazer") {
        listaDeTarefas[posicao].status = "fazendo";
    } else if (listaDeTarefas[posicao].status == "fazendo") {
        listaDeTarefas[posicao].status = "concluido";
    }

    mostrarTarefas();


// exclui a tarefa da lista
function excluirTarefa(id) {

    var novaLista = [];

    for (var i = 0; i < listaDeTarefas.length; i++) {
        if (listaDeTarefas[i].id != id) {
            novaLista.push(listaDeTarefas[i]);
        }
    }

    listaDeTarefas = novaLista;

    mostrarTarefas();
} "
significado dataset



